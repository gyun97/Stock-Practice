# 로컬 환경에서 운영 환경(메모리 1GB, CPU 2 코어 제한 등)을 모사하여 부하 테스트를 진행하기 위한 설정입니다.

services:

  # -----------------
  # MySQL
  # -----------------
  mysql:
    image: mysql:8
    container_name: stock-mysql-local
    environment:
      MYSQL_ROOT_PASSWORD: "${MYSQL_PASSWORD:-1234}"
      MYSQL_DATABASE: "stock"
    ports:
      - "3306:3306"
    volumes:
      - mysql_data_local:/var/lib/mysql
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost" ]
      interval: 5s
      timeout: 5s
      retries: 10

  # -----------------
  # Redis Standalone
  # -----------------
  redis:
    image: bitnami/redis
    container_name: stock-redis-local
    hostname: redis
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    volumes:
      - redis_data_local:/bitnami/redis/data
    ports:
      - "6379:6379"

  # -----------------
  # Nginx Proxy (Local Subdomain)
  # -----------------
  nginx:
    image: nginx:stable-alpine
    container_name: stock-nginx-local
    depends_on:
      - app
    ports:
      - "8888:80"
    volumes:
      - ./frontend/nginx-local.conf:/etc/nginx/conf.d/default.conf:ro

  # -----------------
  # Spring Boot App (Local Prod)
  # -----------------
  app:
    build:
      context: .
      dockerfile: dockerfile
    container_name: stock-app-local
    environment:
      # 운영 프로필 적용. (set1 사용으로 단일 앱 실행 모방)
      - SPRING_PROFILES_ACTIVE=prod,set1
      # JVM 힙 메모리 설정. (전체 700MB 중 384MB를 Heap으로 할당, 나머지 여유)
      - JAVA_TOOL_OPTIONS=-Xmx384m
      # 데이터베이스 및 레디스 호스트명을 덮어씌워서 로컬 컨테이너로 맞춥니다.
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/stock?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
      - SPRING_DATA_REDIS_HOST=redis
      - SPRING_DATA_REDIS_PORT=6379
    env_file:
      - .env.prod
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - "8080:8080"
    deploy:
      resources:
        limits:
          # EC2 micro 환경(총 1GB RAM)을 고려하여 App 자체의 리소스 허용치를 제한합니다.
          cpus: '1.5'
          memory: 600M

  # =================================================
  # 모니터링 스택 (Grafana Cloud 이전)
  # =================================================
  # -----------------
  # Alloy (로그 및 메트릭 수집기)
  # -----------------
  alloy:
    image: grafana/alloy:latest
    container_name: stock-alloy-local
    ports:
      - "12345:12345"
    volumes:
      - ./monitoring/alloy/config.alloy:/etc/alloy/config.alloy:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    env_file:
      - .env.prod
    command:
      - run
      - --server.http.listen-addr=0.0.0.0:12345
      - /etc/alloy/config.alloy
    restart: unless-stopped

volumes:
  mysql_data_local:
  redis_data_local:
