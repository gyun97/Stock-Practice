# ?? ??
server:
  port: 8080
#  ssl:
#    enabled: true

#MySQL ??
spring:
  config:
    import: optional:file:.env[.properties]

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST}:3306/${MYSQL_DATABASE}?serverTimezone=Asia/Seoul
#    url: jdbc:mysql://localhost:3306/stock?serverTimezone=Asia/Seoul
    username: ${MYSQL_NAME}
#    username: root
    password: ${MYSQL_PASSWORD}
#    password: 1234


#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true
#        open-in-view: false